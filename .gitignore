# Local dev artifacts
temporary screenshots/
*.log

# OS
.DS_Store
Thumbs.db

# Local backups exported from the app (keep these out of source unless intended)
frendz-ledger-*.json

# Generated full-ledger snapshot (rebuilt in-browser from the CSV; keep the repo lean)
data/ledger-backup.json

# Node (none required to run, but in case tooling is added)
node_modules/
